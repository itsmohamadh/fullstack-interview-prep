// Merge two arrays with duplicate values

// Write a function that takes two arrays
// Merge them and remove duplicate values

function myFunction(a, b) {
  
}

console.log(myFunction([1, 2], [2, 3])); // [1, 2, 3]
console.log(myFunction(['a'], ['a', 'b'])); // ['a', 'b']
console.log(myFunction([true], [false, true])); // [true, false]