// Define an array with conditional elements

// Write a function that takes a boolean
// Return an array that contains 'apple'
// If the boolean is true, also include 'banana'

function myFunction(condition) {
  
}

console.log(myFunction(true)); // ['apple', 'banana']
console.log(myFunction(false)); // ['apple']